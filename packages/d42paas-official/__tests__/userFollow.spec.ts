// import { shadowUserStore, userListStore, userStore } from '~/stores/daoStore';

// import { DaoPaaS } from '~/bundle';

// mockWorker.start();

describe('UserFollow', () => {
  it('should create an instance', () => {
    // const daopass = new DaoPaaS();
    expect(1 + 2).toBe(3);
  });
});
